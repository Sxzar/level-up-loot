import React from "react";
import "./VideoContainer.css";

const VideoContainer = ({ videos }) => {
	console.log(videos);
	return <div className="lul__videoContainer"></div>;
};

export default VideoContainer;
